apiVersion: v1
kind: Service
metadata:
  namespace: voting-app
  labels:
    app: vote
  name: vote
spec:
  type: ClusterIP # change NodePort to ClusterIP
  ports:
    - name: "vote-service"
      port: 80
      targetPort: 80
      # nodePort: 31000
  selector:
    app: vote
