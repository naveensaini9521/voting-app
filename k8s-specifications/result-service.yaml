apiVersion: v1
kind: Service
metadata:
  namespace: voting-app
  labels:
    app: result
  name: result
spec:
  type: ClusterIP # change NodePort to ClusterIP
  ports:
    - name: "result-service"
      port: 80
      targetPort: 80
      # nodePort: 31001
  selector:
    app: result
